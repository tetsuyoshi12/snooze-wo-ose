{
  "name": "record-manager",
  "version": "1.0.0",
  "description": "Lambda function for managing snooze records in DynamoDB",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
