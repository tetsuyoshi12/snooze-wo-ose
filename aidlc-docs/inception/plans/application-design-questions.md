# アプリケーション設計 - 質問

このファイルの質問にすべて回答してください。各質問の [Answer]: タグの後に、選択肢の文字（A、B、C、D など）を記入してください。提供された選択肢が合わない場合は、最後の選択肢（Other）を選び、その後に説明を記述してください。

すべての質問に回答したら、「完了しました」とお知らせください。

---

## 質問 1: フロントエンドのコンポーネント構成
フロントエンドをどのように構成しますか？

A) ページベース（各画面を独立したページコンポーネントとして構成）
B) 機能ベース（機能ごとにコンポーネントをグループ化）
C) アトミックデザイン（Atoms、Molecules、Organisms、Templates、Pages）
D) レイヤーベース（Presentation、Container、Logic の分離）
E) Other（以下に説明を記述してください）

[Answer]: A

---

## 質問 2: 状態管理の方法
アプリケーションの状態管理をどのように行いますか？

A) React Context API のみ
B) Redux または Redux Toolkit
C) Zustand または Jotai などの軽量ライブラリ
D) ローカルストレージ + React State のみ（サーバレス、シンプル）
E) Other（以下に説明を記述してください）

[Answer]: D

---

## 質問 3: バックエンド API の構成
バックエンド API をどのように構成しますか？

A) 単一の Lambda 関数（すべての API エンドポイントを 1 つの関数で処理）
B) 機能ごとに Lambda 関数を分離（アラーム、AI メッセージ、記録など）
C) レイヤーアーキテクチャ（API Gateway → Lambda → Service Layer → Data Layer）
D) Other（以下に説明を記述してください）

[Answer]: B

---

## 質問 4: AI メッセージ生成の責務配置
AI メッセージ生成の責務をどこに配置しますか？

A) フロントエンドから直接 Bedrock を呼び出す
B) Lambda 関数で Bedrock を呼び出し、フロントエンドは Lambda 経由でアクセス
C) 専用の AI サービスレイヤーを作成し、Lambda から呼び出す
D) Other（以下に説明を記述してください）

[Answer]: B

---

## 質問 5: データ永続化の戦略
データ永続化をどのように行いますか？

A) ローカルストレージのみ（シンプル、コスト削減）
B) DynamoDB のみ（クラウド永続化、複数デバイス対応）
C) ハイブリッド（ローカルストレージ + DynamoDB、オフライン対応）
D) Other（以下に説明を記述してください）

[Answer]: C

---

## 質問 6: アラーム機能の実装方法
ブラウザでのアラーム機能をどのように実装しますか？

A) JavaScript の setTimeout/setInterval のみ
B) Service Worker + Background Sync API
C) Web Notifications API + setTimeout
D) Other（以下に説明を記述してください）

[Answer]: C

---

## 質問 7: コンポーネント間の通信パターン
コンポーネント間の通信をどのように行いますか？

A) Props drilling（親から子へ props を渡す）
B) Context API でグローバル状態を共有
C) イベントバス（カスタムイベント）
D) 状態管理ライブラリ（Redux、Zustand など）
E) Other（以下に説明を記述してください）

[Answer]: B

---

## 質問 8: UI デザインシステムの採用
墨絵風 UI をどのように実装しますか？

A) カスタム CSS のみ（完全に独自デザイン）
B) UI ライブラリ（Material-UI、Chakra UI など）をカスタマイズ
C) Tailwind CSS + カスタムコンポーネント
D) CSS-in-JS（styled-components、Emotion など）
E) Other（以下に説明を記述してください）

[Answer]: C

---

## 質問 9: ルーティングの必要性
アプリケーションにルーティングが必要ですか？

A) はい、複数ページ（設定、アラーム、記録、履歴など）
B) はい、シングルページだがモーダル/タブで画面切り替え
C) いいえ、シングルページアプリケーション（SPA）で画面遷移なし
D) Other（以下に説明を記述してください）

[Answer]: B

---

## 質問 10: エラーハンドリングの責務
エラーハンドリングをどこで行いますか？

A) 各コンポーネントで個別に処理
B) グローバルエラーバウンダリ + 個別処理
C) サービスレイヤーで集中管理
D) Lambda 関数で処理し、フロントエンドにはエラーメッセージのみ返す
E) Other（以下に説明を記述してください）

[Answer]: B

---

## 質問 11: デモモードの実装方法
デモ用ライブモードをどのように実装しますか？

A) フラグで通常モードとデモモードを切り替え
B) 専用のデモコンポーネント/ページを作成
C) 環境変数でデモデータを注入
D) Other（以下に説明を記述してください）

[Answer]: A

---

## 質問 12: ゲーミフィケーション要素の管理
堕落ゲージ、連続記録、称号などのゲーミフィケーション要素をどこで管理しますか？

A) フロントエンドの状態管理のみ
B) バックエンドで計算し、フロントエンドは表示のみ
C) ハイブリッド（フロントエンドで計算、バックエンドで永続化）
D) Other（以下に説明を記述してください）

[Answer]: C

---

## 質問 13: API エンドポイントの粒度
API エンドポイントをどの程度の粒度で設計しますか？

A) 粗粒度（少数の汎用エンドポイント、例: /api/data）
B) 中粒度（機能ごとのエンドポイント、例: /api/alarm、/api/message、/api/record）
C) 細粒度（操作ごとのエンドポイント、例: /api/alarm/set、/api/alarm/snooze、/api/alarm/dismiss）
D) Other（以下に説明を記述してください）

[Answer]: B

---

## 質問 14: 認証・認可の必要性
このアプリケーションに認証・認可が必要ですか？

A) 必要（ユーザー登録とログイン）
B) 不要（ローカルストレージベース、個人利用）
C) 将来的に必要（MVP では不要、ハッカソン後に追加）
D) Other（以下に説明を記述してください）

[Answer]: B

---

## 質問 15: インフラストラクチャのコード管理
インフラストラクチャをどのように管理しますか？

A) AWS CDK（TypeScript/Python）
B) AWS CloudFormation（YAML/JSON）
C) Terraform
D) 手動設定（AWS コンソール）
E) AWS Amplify CLI（自動生成）
F) Other（以下に説明を記述してください）

[Answer]: A

---

すべての質問に回答したら、「完了しました」とお知らせください。
回答を確認し、曖昧な点があれば追加の質問をさせていただきます。
